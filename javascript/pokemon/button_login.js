// Selecciona el botón con la clase "icon-button"
const iconButton = document.querySelector('.icon-button');

// Agrega un detector de eventos 'click' al botón
iconButton.addEventListener('click', function(event) {
  // Evita el comportamiento predeterminado del clic del botón
  event.preventDefault();
});


